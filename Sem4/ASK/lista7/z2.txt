a)
Wersja bez zamian kolejnosci:

1 mov (%rax), %rdi       
nop
nop              
2 add %rdi, %rsi    # Czeka 2 cykle na %rdi z instrukcji 1
3 imul %rcx, %rax, %rdi
nop                 4 linia potrzebuje rsi i ma 2 przerwy wstawiamy nopa i 3 linia daje nam drugie
4 mov %rsi, (%rcx)
5 and %rcx, %rdi     5 linia potrzebuje rdi z trzeciej ale poprzedni nop i sama 4 linia wyrównują
6 mov %rcx, %rdi

ilosc nopów: 3

Wersja ze zmianami:

//mozemy miec dwie osobne linie 1-2-4 i 3-5
1 mov (%rax), %rdi
3 imul %rcx, %rax, %r8 odciazamy rdi i uzywamy wolnego rejestru r8 by jednoczesnie wstawic opoznienie i uratowac jednego nopa i odciazyc rdi
nop
2 add %rdi, %rsi
5 and %rcx, %r8  // teraz juz mozemy uzyc 5 linii z r8 która ma wyrównane dwa opoznienia 
6 mov %rcx, %rdi //rcx jest gotowe i mozemy bezpiecznie nadpisac rdi i jednoczesnie tracimy nopa pomiedzy 2-4
4 mov %rsi, (%rcx) //rsi juz jest dostepne

ilosc nopów: 1

b)
Wersja bez zamian kolejnosci:

1 add %rcx, %rdi
2 mov %rdi, (%rsi)
3 mov 8(%rsi), %rdi
nop
nop                       add musi odczytać rdi wiec potrzebujemy dwóch nopów by zrównąć WB mova z ID adda
4 add %rdi, %rcx
5 mov %rdi, 16(%rsi)
nop                     mov musi odczytać wartosc w ID ale add musi zdazyc ją zapisać w WB 5 linia tworzy naturalne przesuniecie
6 mov %rcx, %rdi

ilosc nopów: 3

Wersja ze zmianami:

3 mov 8(%rsi), %r8
1 add %rcx, %rdi
2 mov %rdi, (%rsi)
4 add %r8, %rcx     teraz mamy dwa przesuniecia juz pomiedzy trzecią a 4 linią i używamy r8 by je oderwać od reszty
5 mov %rdi, 16(%rsi)
nop                 tutaj nop zostaje
6 mov %rcx, %rdi

ilosc nopów: 1



IF: Instruction Fetch  pobranie instrukcji z pami¦ci
2. ID: Instruction Decode  pobranie warto±ci z rejestrów
3. EX: Execute  wykonanie operacji arytmetyczno-logicznych
4. MEM: Memory access  odczyty z/zapisy do pami¦ci
5. WB: Write Back  zapis warto±ci do rejestrów